import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  boxcard: {
    display: "flex",
    justifyContent: "space-between",
  },
  techstack: {
    textAlign: "center",
  },
}));
